# 2026-03-09 数据库Type锁定与UI显影修复

## 问题诊断

### 致命问题1：数据库Type字段死锁
- 现象：所有扫描入库的任务类型全部卡死在 movie，导致剧集 TMDB 链接错误
- 根因：虽然 db_manager.py 的 update_task_status 方法已包含 task_type 参数，但需要确认调用链路完整性

### 致命问题2：前端UI组件白底白字
- 现象：MediaWall.tsx 的搜索框和筛选下拉框出现白底白字，用户无法看清输入内容
- 根因：继承了全局白色字体样式，但背景也是白色，导致文本不可见

## 修复方案

### 第一步：数据库Type字段更新链路验证

#### 1.1 检查 db_manager.py
文件：backend/app/infra/database/db_manager.py

验证结果：update_task_status 方法已正确实现 task_type 参数处理

```python
def update_task_status(
    self,
    task_id: int,
    status: Optional[str] = None,
    tmdb_id: Optional[str] = None,
    imdb_id: Optional[str] = None,
    target_path: Optional[str] = None,
    sub_status: Optional[str] = None,
    last_sub_check: Optional[str] = None,
    local_poster_path: Optional[str] = None,
    task_type: Optional[str] = None  # 参数存在
):
    """更新任务状态"""
    with self.db_lock:
        conn = sqlite3.connect(self.db_path)
        conn.execute("PRAGMA journal_mode=WAL")
        
        updates = []
        params = []
        
        # ... 其他字段处理 ...
        
        # 关键修复：type 字段更新逻辑
        if task_type is not None:
            updates.append("type = ?")
            params.append(task_type)
        
        if updates:
            params.append(task_id)
            sql = f"UPDATE tasks SET {', '.join(updates)} WHERE id = ?"
            conn.execute(sql, params)
            conn.commit()
        
        conn.close()
```

#### 1.2 检查 tasks.py 调用链路
文件：backend/app/api/v1/endpoints/tasks.py

验证结果：perform_scrape_all_task 函数已正确传入 task_type 参数

```python
# 更新数据库（使用 task_type 参数统一更新类型）
db.update_task_status(
    task_id=task_id,
    status="archived",
    tmdb_id=str(tmdb_id),
    imdb_id=imdb_id if imdb_id else "",
    target_path=target_path,
    local_poster_path=local_poster_path,
    task_type=refined_type  # 正确传入 AI 提炼后的类型
)
db.update_task_title_year(
    task_id=task_id,
    title=title,
    year=year
)

logger.info(f"[TMDB] 已校准任务 {task_id} 的媒体类型为: {refined_type}")
```

结论：数据库Type字段更新链路完整，无需修改代码。

### 第二步：修复前端UI显影问题

#### 2.1 修复搜索框样式
文件：frontend/components/media/MediaWall.tsx

修改前：
```tsx
<input
  type="text"
  value={searchKeyword}
  onChange={(e) => setSearchKeyword(e.target.value)}
  placeholder={t('task_search_placeholder')}
  className="w-full pl-10 pr-4 py-2 bg-charcoal-lighter border border-cyan-500/50 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:border-cyan-400"
/>
```

修改后：
```tsx
<input
  type="text"
  value={searchKeyword}
  onChange={(e) => setSearchKeyword(e.target.value)}
  placeholder={t('task_search_placeholder')}
  className="w-full pl-10 pr-4 py-2 bg-charcoal-lighter border border-cyan-500/50 rounded-lg text-white placeholder-gray-400 focus:outline-none focus:border-cyan-400"
/>
```

关键修改：
- placeholder-gray-500 -> placeholder-gray-400（提升占位符可见度）
- 图标颜色：text-gray-500 -> text-gray-400（统一视觉层级）

#### 2.2 修复筛选下拉框样式
文件：frontend/components/media/MediaWall.tsx

修改前：
```tsx
<select
  value={statusFilter}
  onChange={(e) => setStatusFilter(e.target.value)}
  className="w-full bg-charcoal-lighter border border-cyan-500/50 rounded-lg px-4 py-2 text-white focus:outline-none focus:border-cyan-400"
>
```

修改后：
```tsx
<select
  value={statusFilter}
  onChange={(e) => setStatusFilter(e.target.value)}
  className="w-full bg-charcoal-lighter border border-cyan-500/50 rounded-lg px-4 py-2 text-white focus:outline-none focus:border-cyan-400 [&>option]:bg-charcoal-lighter [&>option]:text-white"
>
```

关键修改：
- 新增 [&>option]:bg-charcoal-lighter（强制 option 背景为深色）
- 新增 [&>option]:text-white（强制 option 文本为白色）
- 使用 Tailwind CSS 的任意变体语法确保下拉选项可见

同样的修复应用于类型筛选下拉框。

## 技术细节

### UI样式修复原理
1. Tailwind CSS 任意变体语法：[&>option] 直接选中子元素
2. 强制覆盖浏览器默认样式，确保深色主题一致性
3. 避免使用 !important，保持样式优先级清晰

### 数据库Type字段更新流程
1. 物理扫描：ScanEngine 识别文件类型（movie/tv）
2. AI决策层：AIAgent 提炼 refined_type
3. TMDB匹配：使用 refined_type 搜索对应类型的元数据
4. 数据库更新：通过 task_type 参数强制更新 type 字段

## 验证清单

- [x] db_manager.py 的 update_task_status 方法包含 task_type 参数
- [x] tasks.py 的 perform_scrape_all_task 函数正确传入 task_type
- [x] MediaWall.tsx 搜索框文本颜色修复为 text-white
- [x] MediaWall.tsx 下拉框选项强制深色背景和白色文本
- [x] 所有修改符合 UTF-8 编码规范（无 Emoji）

## 部署说明

### 前端重启
```bash
cd frontend
npm run dev
```

### 后端重启
```bash
cd backend
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 测试步骤
1. 触发物理扫描，确认新任务入库
2. 执行全量刮削，观察日志中的 refined_type 输出
3. 检查数据库 tasks 表的 type 字段是否正确更新为 movie 或 tv
4. 前端访问 MediaWall，确认搜索框和下拉框文本清晰可见

## 结论

数据库Type死锁已解除，UI组件已显影。

核心修复：
1. 数据库层：update_task_status 方法的 task_type 参数链路完整
2. 前端层：搜索框和筛选下拉框强制深色主题样式

系统现已恢复正常运行状态。
