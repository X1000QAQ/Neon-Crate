# 寻猎者 V9.2 升级验证清单

## ✅ 第一步：TMDB 侦察兵引入

**文件**: `backend/app/services/downloader/servarr.py`

- [x] 导入 `TMDBAdapter` 
- [x] 新增 `_tmdb_recon` 方法
- [x] 支持 movie 和 tv 两种类型
- [x] 返回 `{"tmdbId": int, "title": str, "year": str}`
- [x] 从 `db.get_config("tmdb_api_key")` 获取 API Key
- [x] 如果未配置 API Key，返回 None 并记录错误日志

**验证代码位置**: 第 39-95 行

---

## ✅ 第二步：基于 TMDB ID 的精准查找与指纹校验

**文件**: `backend/app/services/downloader/servarr.py`

### add_movie 方法 (第 97-245 行)

- [x] 步骤 0: 调用 `_tmdb_recon` 获取 TMDB 数据
- [x] 步骤 A: 使用 `tmdb:{tmdb_id}` 语法精准搜索
- [x] 步骤 A+: 使用 `SequenceMatcher` 计算相似度
- [x] 相似度阈值设置为 0.6 (60%)
- [x] 相似度过低时拒绝下载并返回错误信息

**关键代码**:
```python
search_term = f"tmdb:{tmdb_info['tmdbId']}"
similarity = SequenceMatcher(None, radarr_title.lower(), tmdb_info['title'].lower()).ratio()
if similarity < 0.6:
    return {"success": False, "msg": "匹配度过低，拒绝下载", "data": {}}
```

### add_series 方法 (第 247-395 行)

- [x] 步骤 0: 调用 `_tmdb_recon` 获取 TMDB 数据
- [x] 步骤 A: 使用 `tmdb:{tmdb_id}` 语法精准搜索
- [x] 步骤 A+: 使用 `SequenceMatcher` 计算相似度
- [x] 相似度阈值设置为 0.6 (60%)
- [x] 相似度过低时拒绝下载并返回错误信息

---

## ✅ 第三步：优化"已存在"拦截

**文件**: `backend/app/services/downloader/servarr.py`

### add_movie 方法

- [x] 捕获 `httpx.HTTPStatusError` 异常
- [x] 检测 `status_code == 400`
- [x] 检查错误信息中是否包含 "already been added"
- [x] 返回格式: `{"success": False, "msg": "[OK] {title} 已在库中监控补全。", "data": {"status": "exists"}}`

**验证代码位置**: 第 225-236 行

### add_series 方法

- [x] 捕获 `httpx.HTTPStatusError` 异常
- [x] 检测 `status_code == 400`
- [x] 检查错误信息中是否包含 "already been added"
- [x] 返回格式: `{"success": False, "msg": "[OK] {title} 已在库中监控补全。", "data": {"status": "exists"}}`

**验证代码位置**: 第 375-386 行

---

## ✅ 第四步：严格执行 AI 人格对齐

**文件**: `backend/app/services/ai/agent.py`

**用户人格设定**: "直接输出结果，严禁说废话，严禁任何解释性语言"

### DOWNLOAD 意图处理

- [x] 成功时: `"[寻猎者] 已下发下载指令: {title}"`
- [x] 已存在时: 使用 servarr 返回的消息 `result["msg"]`
- [x] 失败时: `"[寻猎者] 匹配失败: {result['msg']}"`
- [x] 移除所有啰嗦的解释性语言
- [x] 移除所有 Emoji 字符（Windows 兼容）

**验证代码位置**: agent.py 第 255-295 行

**修改前**:
```python
return f"✅ 成功寻获！已将《{title}》下发至下载器。请耐心等待下载完成，系统会自动为您整理入库。"
```

**修改后**:
```python
response_text = f"[寻猎者] 已下发下载指令: {title}"
```

---

## ✅ 第五步：TMDB API Key 获取逻辑

**文件**: `backend/app/services/downloader/servarr.py`

- [x] 在 `_tmdb_recon` 方法中获取 API Key
- [x] 使用 `self.db.get_config("tmdb_api_key", "").strip()`
- [x] 如果为空，记录错误日志并返回 None
- [x] 拦截后续流程，防止无效请求

**验证代码位置**: 第 51-54 行

---

## ✅ 安全红线检查

- [x] 保持 `encoding="utf-8"` (Windows 兼容)
- [x] 严禁移除现有的 Sonarr/Radarr API 鉴权代码
- [x] 所有日志输出使用纯 ASCII 字符（无 Emoji）
- [x] Python 语法验证通过 (`python -m py_compile`)

---

## ✅ 代码质量检查

- [x] 无 Linter 错误
- [x] 无语法错误
- [x] 导入语句正确
- [x] 类型提示完整 (`Optional[Dict]`, `Dict`)
- [x] 异常处理完善
- [x] 日志记录规范

---

## 📋 测试建议

### 1. TMDB 侦察测试
```python
# 测试电影搜索
result = servarr._tmdb_recon("乱马 1/2", "movie", "2024")
assert result is not None
assert result["tmdbId"] > 0

# 测试剧集搜索
result = servarr._tmdb_recon("进击的巨人", "tv", "2013")
assert result is not None
assert result["tmdbId"] > 0
```

### 2. 指纹校验测试
```python
# 测试相似度计算
from difflib import SequenceMatcher
similarity = SequenceMatcher(None, "Ranma 1/2", "Ranma ½").ratio()
assert similarity >= 0.6
```

### 3. 已存在拦截测试
- 重复添加同一部作品
- 验证返回消息: `"[OK] {title} 已在库中监控补全。"`
- 验证 `data.status == "exists"`

### 4. 极简人格测试
- 下载成功: 验证回复是否为 `"[寻猎者] 已下发下载指令: {title}"`
- 下载失败: 验证回复是否为 `"[寻猎者] 匹配失败: {msg}"`
- 已存在: 验证回复是否为 `"[OK] {title} 已在库中监控补全。"`

---

## 🎯 升级完成标志

**寻猎者已升级指纹对齐，AI人格已进入极简模式**

---

**验证日期**: 2026-03-09  
**验证人**: 首席全栈系统架构师  
**验证结果**: ✅ 全部通过
