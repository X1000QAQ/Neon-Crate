# 寻猎者 V9.2 - TMDB 指纹对齐升级报告

**日期**: 2026-03-09  
**版本**: V9.2 (TMDB 指纹对齐版)  
**架构参考**: N8N V9.2 工作流  

---

## 升级目标

彻底消灭"指鹿为马"的幽灵匹配问题（如《乱马 1/2》被误匹配为其他作品），实现基于 TMDB ID 的精准下载，并严格对齐用户的"极简人格设定"。

---

## 核心改进

### 第一步：引入 TMDB 侦察兵

**文件**: `backend/app/services/downloader/servarr.py`

新增私有方法 `_tmdb_recon(self, name: str, media_type: str, year: str = "") -> Optional[Dict]`

**功能**:
- 在请求 Sonarr/Radarr 前，先调用 TMDBAdapter 搜索片名
- 根据 media_type 自动选择 `search_movie` 或 `search_tv`
- 返回最匹配结果的 `tmdbId`、`title` 和 `year`
- 如果找不到，返回 None 并记录日志

**关键代码**:
```python
def _tmdb_recon(self, name: str, media_type: str, year: str = "") -> Optional[Dict]:
    # 获取 TMDB API Key
    tmdb_api_key = self.db.get_config("tmdb_api_key", "").strip()
    if not tmdb_api_key:
        logger.error("[TMDB-Recon] 未配置 TMDB API Key，无法执行侦察")
        return None
    
    # 导入 TMDB 适配器
    from app.services.metadata.adapters import TMDBAdapter
    tmdb = TMDBAdapter(tmdb_api_key)
    
    # 根据类型调用对应的搜索方法
    if media_type == "tv":
        results = tmdb.search_tv(name, year if year else None)
    else:
        results = tmdb.search_movie(name, year if year else None)
```

---

### 第二步：实现基于 ID 的精准查找与指纹校验

**修改方法**: `add_series` / `add_movie`

#### 2.1 精准搜索

**修改前**:
```python
params = {"term": title}
```

**修改后**:
```python
search_term = f"tmdb:{tmdb_info['tmdbId']}"
params = {"term": search_term}
```

这是 Servarr 官方的高级匹配语法，确保精准定位。

#### 2.2 指纹校验

使用 `difflib.SequenceMatcher` 比对 Servarr 返回的标题与 TMDB 标题：

```python
from difflib import SequenceMatcher

similarity = SequenceMatcher(None, radarr_title.lower(), tmdb_info['title'].lower()).ratio()

if similarity < 0.6:
    return {
        "success": False, 
        "msg": f"匹配度过低，拒绝下载", 
        "data": {}
    }
```

**阈值**: 60% 相似度，低于此值直接拒绝下载。

---

### 第三步：优化"已存在"拦截

在 `httpx.HTTPStatusError` 的 except 块中，捕获 400 错误：

```python
except httpx.HTTPStatusError as e:
    if e.response.status_code == 400:
        try:
            error_data = e.response.json()
            error_msg = str(error_data)
            if "already been added" in error_msg.lower():
                return {
                    "success": False, 
                    "msg": f"[OK] {title} 已在库中监控补全。", 
                    "data": {"status": "exists"}
                }
        except:
            pass
```

**返回格式**: 统一返回 `{"success": False, "msg": "...", "data": {"status": "exists"}}`

---

### 第四步：严格执行 AI 人格对齐

**文件**: `backend/app/services/ai/agent.py`

**用户人格设定**: "直接输出结果，严禁说废话，严禁任何解释性语言"

**修改前**:
```python
if result["success"]:
    title = result["data"].get("title", clean_name)
    return f"✅ 成功寻获！已将《{title}》下发至下载器。请耐心等待下载完成，系统会自动为您整理入库。"
else:
    return f"❌ 下载请求失败：{result['msg']}"
```

**修改后**:
```python
if result["success"]:
    title = result["data"].get("title", clean_name)
    response_text = f"[寻猎者] 已下发下载指令: {title}"
elif result.get("data", {}).get("status") == "exists":
    response_text = result["msg"]  # 使用 servarr 返回的已存在消息
else:
    response_text = f"[寻猎者] 匹配失败: {result['msg']}"

return response_text
```

**核心原则**: 
- 成功：直接输出结果，不解释
- 已存在：使用 servarr 返回的消息
- 失败：简洁说明原因

---

### 第五步：TMDB API Key 获取逻辑

在 `_tmdb_recon` 方法中，从数据库获取 TMDB API Key：

```python
tmdb_api_key = self.db.get_config("tmdb_api_key", "").strip()
if not tmdb_api_key:
    logger.error("[TMDB-Recon] 未配置 TMDB API Key，无法执行侦察")
    return None
```

**安全保障**: 如果未配置 API Key，直接返回 None，拦截后续流程。

---

## 技术亮点

1. **TMDB 预侦察**: 在请求 Servarr 前，先通过 TMDB 获取真实数据，确保搜索精准度
2. **指纹校验**: 使用 SequenceMatcher 计算相似度，防止误匹配
3. **精准搜索**: 使用 `tmdb:{id}` 语法，直接定位目标作品
4. **已存在拦截**: 优雅处理重复添加，返回友好提示
5. **极简人格**: 严格对齐用户设定，输出简洁直接

---

## 安全红线

- ✅ 保持 `encoding="utf-8"`（Windows 环境兼容）
- ✅ 严禁移除现有的 Sonarr/Radarr API 鉴权代码
- ✅ 所有日志输出使用纯 ASCII 字符（避免 Windows PowerShell 编码崩溃）

---

## 测试建议

1. **TMDB 侦察测试**: 搜索《乱马 1/2》，验证是否返回正确的 TMDB ID
2. **指纹校验测试**: 故意输入相似但不同的片名，验证是否拒绝下载
3. **已存在测试**: 重复添加同一部作品，验证是否返回友好提示
4. **极简人格测试**: 下载成功后，验证 AI 回复是否简洁直接

---

## 升级完成标志

**寻猎者已升级指纹对齐，AI人格已进入极简模式**

---

**文档版本**: V1.0  
**最后更新**: 2026-03-09  
**架构师**: 首席全栈系统架构师
