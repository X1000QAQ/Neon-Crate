# Neon Crate 前端架构白皮书

**文档编号**：ARCH-003  
**版本**：v1.0.0-Stable  
**最后更新**：2026-03-14  

---

## 一、系统定位与设计哲学

Neon Crate 前端是「数据容器编排引擎的工业级霓虹控制面板」。

| 原则 | 具体体现 |
|------|----------|
| **类型即契约** | `types/index.ts` 唯一类型来源，与后端 Pydantic 严格对齐 |
| **单一数据入口** | 所有 HTTP 调用只能经过 `lib/api.ts`，禁止裸 `fetch` |
| **国际化优先** | 所有 UI 字符串通过 `t(key)` 获取，严禁硬编码 |
| **错误防线纵深** | `apiError.ts` → `NeuralLinkAlert` → `error.tsx` 三层递进 |
| **前端不存业务规则** | 正则/AI 规则来自后端数据库，前端只展示与提交 |

---

## 二、技术栈

| 分类 | 技术 |
|------|------|
| 框架 | Next.js 14（App Router）+ TypeScript |
| 样式 | Tailwind CSS + CSS 变量（霓虹主题）|
| 字体 | Advent Pro + Share Tech Mono |
| HTTP | 原生 Fetch 封装于 `lib/api.ts` |
| 状态 | useState / Context |
| i18n | 自研（`lib/i18n.ts`，< 100 行，中英双语）|

---

## 三、物理目录结构

```
frontend/
├── app/
│   ├── layout.tsx            # 根布局（Server Component）
│   ├── page.tsx              # 主界面（activeView 四视图切换）
│   ├── globals.css           # CSS 变量霓虹主题
│   ├── error.tsx             # 全局错误边界
│   └── auth/login/page.tsx   # 登录/初始化双模态
├── components/
│   ├── ai/AiSidebar.tsx
│   ├── common/
│   │   ├── AuthGuard.tsx
│   │   ├── ClientShell.tsx
│   │   ├── CyberParticles.tsx
│   │   ├── NeuralLinkAlert.tsx
│   │   └── SecureImage.tsx
│   ├── media/
│   │   ├── MediaWall.tsx
│   │   ├── MediaTable.tsx
│   │   ├── MediaToolbar.tsx
│   │   ├── MediaPagination.tsx
│   │   ├── MiniLog.tsx
│   │   ├── StatsOverview.tsx
│   │   └── SystemMonitor.tsx
│   └── settings/
│       ├── SettingsHub.tsx
│       ├── BasicSettings.tsx
│       ├── PathsSettings.tsx
│       ├── APISettings.tsx
│       ├── InferenceSettings.tsx
│       ├── PersonaSettings.tsx
│       ├── RegexLab.tsx
│       └── NeuralPrimitives.tsx
├── context/NetworkContext.tsx
├── hooks/useLanguage.ts
├── lib/
│   ├── api.ts
│   ├── apiError.ts
│   ├── config.ts
│   ├── i18n.ts
│   └── utils.ts
└── types/index.ts
```

---

## 四、应用初始化

### ClientShell

```
ClientShell（'use client'）
  └─► NetworkProvider
        ├─► CyberParticles      （fixed, z-0）
        ├─► AuthGuard
        │     ├─► {children}
        │     └─► AiSidebar
        └─► NeuralLinkAlert     （z-[9999]）
```

### 主页视图切换

| activeView | 渲染组件 |
|------------|----------|
| `dashboard` | `StatsOverview` + `MiniLog` |
| `media` | `MediaWall` |
| `monitor` | `SystemMonitor` |
| `settings` | `SettingsHub` |

---

## 五、认证与鉴权

```
1. 路径为 /auth/login → 直接放行
-> 2. api.authStatus() → 系统未初始化 → 跳转初始化模式
-> 3. localStorage.token 不存在 → 跳转登录模式
-> 4. 通过 → 渲染 {children} + AiSidebar
```

**定时器生命周期加固**（AuthGuard + StatsOverview）：
```typescript
// 1. 注册追踪：setTimeout ID 集中存入 useRef 数组
// -> 2. 静默清理：useEffect cleanup 遍历 clearTimeout
// -> 3. 预防报错：防止卸载后更新已不存在的 state
const timerRefs = useRef<ReturnType<typeof setTimeout>[]>([]);
useEffect(() => { return () => { timerRefs.current.forEach(clearTimeout); }; }, []);
```

---

## 六、数据获取层（lib/api.ts）

### secureFetch 核心拦截器

```
// 异步链路治理：
// 1. 注入超时 AbortSignal：timeoutMs 后自动 abort
// -> 2. 超时物理掐断：AbortError → REQUEST_TIMEOUT（区分超时与用户主动取消）
// -> 3. 复航信号：非 5xx 自动解除全局网络警告横幅
```

### api.chat 的 AbortController 全链路

```
前端 AiSidebar：
  1. 注入信号：创建新 AbortController，signal 传入 fetch
  -> 2. 物理掐断：连续发送时 abort() 旧请求，浏览器切断连接
  -> 3. 静默处理：catch AbortError 直接 return，不展示错误气泡

后端 agent.py：
  异步链路治理步骤 3：Uvicorn 感知连接断开注入 CancelledError
  → 释放 LLM 推理资源，必须 re-raise
```

### API 方法速览

| 分类 | 方法 | 端点 |
|------|------|------|
| 鉴权 | `authStatus / initAuth / login` | `/auth/*` |
| 任务 | `getTasks / deleteTask / retryTask` | `/tasks/*` |
| 触发 | `triggerScan / triggerScrapeAll / triggerFindSubtitles` | `/tasks/*` |
| 配置 | `getSettings / updateSettings / resetSettings` | `/tasks/settings` |
| 系统 | `getStats / getSystemLogs / chat` | `/system/* /agent/*` |

---

## 七、状态管理

### NetworkContext — 全局断链状态

```
NetworkProvider 挂载 → 注册 window.__setLinkDown = setIsLinkDown
api.ts 检测异常 → notifyLinkDown() → window.__setLinkDown?.(true)
  → NeuralLinkAlert 弹窗显示
NetworkProvider 卸载 → delete window.__setLinkDown（防内存泄漏）
```

### SettingsHub — 配置状态

六个 Tab 共享同一 `config` 对象：

| Tab | 组件 | 职责 |
|-----|------|------|
| basic | BasicSettings | 语言/扫描/定时巡逻 |
| paths | PathsSettings | 下载目录 + 媒体库（1+1 约束）|
| api | APISettings | TMDB/OS/Radarr/Sonarr 密钥 |
| inference | InferenceSettings | 云端/本地 LLM 配置 |
| persona | PersonaSettings | AI 名称/人设/路由规则 |
| regex | RegexLab | 正则清洗规则编辑器 |

**渲染性能优化**（InferenceSettings）：
```typescript
// 1. 本地拦截：击键时仅更新 localState，阻断 SettingsContext 全局重绘
// -> 2. 最终同步：onBlur 时才调用 updateSetting 提交全局配置
// -> 3. 体验加固：Context 重绘代价高，高频输入必须本地缓冲后批量提交
```

---

## 八、AI 交互前端舱（AiSidebar.tsx）

### 候选列表解析

后端 DOWNLOAD 意图返回模糊结果时，响应末尾附加：
```
引导语 __CANDIDATES__["片名 (年份)", ...]
```
`parseCandidates()` 以 `__CANDIDATES__` 为分隔符切割，渲染为快捷选择按钮。

### 候选封死机制

- 只有**最后一条含候选的消息**按钮处于激活状态
- 任何新消息发送时立即封死所有旧候选（`disabled` + 变灰）
- 候选按钮点击 + `handleSendText` 双重保险，防止重复触发

---

## 九、错误处理体系

```
第一层：lib/apiError.ts
  → ApiError（code + status）+ notifyLinkDown()

第二层：NeuralLinkAlert
  → 捕获 5xx / 超时 / 断网 → 全屏弹窗
  → 「重连」→ setLinkDown(false) + reload()

第三层：app/error.tsx
  → Next.js Error Boundary → 赛博朋克风格错误页
```

| ApiError code | 触发场景 | status |
|---------------|----------|--------|
| `REQUEST_TIMEOUT` | 15s 超时 | 408 |
| `NETWORK_ERROR` | 网络不可达 | 0 |
| `UNAUTHORIZED` | 401 响应 | 401 |
| `SERVER_ERROR` | 5xx 响应 | 500+ |

---

## 十、主题与样式系统

| CSS 变量 | 语义 | 值 |
|----------|------|----|
| `--cyber-cyan` | 主色调（霓虹青）| `#00e6f6` |
| `--cyber-yellow` | 强调色 | `#f5e642` |
| `--cyber-red` | 危险/错误 | `#ff3c5a` |
| `--cyber-green` | 成功 | `#00ff9f` |
| `--bg-primary` | 主背景 | `#0a0e1a` |

**规范**：颜色统一引用 CSS 变量，禁止在组件中出现离散色值（如 `#00e6f6`）。

---

## 十一、架构约束与红线

| 约束 | 说明 |
|------|------|
| 禁止裸 fetch | 所有 HTTP 调用必须经过 `lib/api.ts` |
| 禁止硬编码类型 | Task.status / sub_status 必须从 `types/index.ts` 引用 |
| 禁止硬编码正则 | 清洗规则来自后端 `filename_clean_regex` |
| 禁止硬编码字符串 | 所有 UI 文本通过 `t(key)` 获取 |
| 禁止离散色值 | 颜色统一使用 CSS 变量 |
| 1+1 媒体库约束 | 电影媒体库 ≤ 1，剧集媒体库 ≤ 1 |
| 图片路径必须代理 | 本地图片经 `SecureImage` 组件代理 |
| i18n key 必须双语 | zh 和 en 字典必须同步添加 |

*Neon Crate 前端架构组 | v1.0.0-Stable | 2026-03-14*
```
