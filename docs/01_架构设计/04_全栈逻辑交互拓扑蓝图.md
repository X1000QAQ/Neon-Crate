# 全栈逻辑交互拓扑蓝图

**文档编号**：ARCH-004（原 DEV-ARCH-001，文件名沿用仓库路径 `04_全栈逻辑交互拓扑蓝图.md`）  
**最后更新**：2026-03-20  
**状态**：与当前刮削 / 鉴权 / 图片代理代码一致；手动补录与全量重建见 `rebuilder` 与 `02_API规范与鉴权.md` 第四节

---

## 📋 文档概述

本文档基于全栈源码级注释，使用 Mermaid.js 绘制核心业务流程图，展示 Neon-Crate 系统的完整交互拓扑。

> **v1.0.0-Atomic 新增**：刮削流水线现已引入「原子级识别协议」，在 AI 分析之前物理锁定季集号，彻底消除集数信息被正则误删的根因问题。

---

## 🛡️ 原子级识别协议 (Atomic Identification Protocol)

> 本节描述 v1.0.0-Atomic 引入的核心识别机制升级，解决了剧集集数被正则误删导致 IMDb 查重熔断的根因问题。

### 问题根因

以文件名 `Joy of Life 2019 S01 E01 1080p WEB-DL AVC AAC LC-MayiWeb.mp4` 为例：

- `S01` 与 `E01` 之间有**空格**，旧规则 10 `[Ss](\d{1,2})[Ee](\d{1,3})` 无法匹配此格式
- 规则 13 `[Ee][Pp]?[\s\._-]*(\d{1,3})` 单独命中 `E01` 并将其**物理删除**
- AI 收到的已是不含集号的残缺文件名，无法提取 Episode
- 后端防重检测缺少集号 → 触发 IMDb 查重熔断 → 误标为 `ignored`

### 三层修复协议

#### 层 1：[EXTRACT] 规则隔离

`filename_clean_regex` 规则库现支持**行为标记**：注释行中含 `[EXTRACT]` 的规则为**提取专用规则**，仅参与结构化提取，**不参与** `clean_name()` 物理删除。

```
# --- 规则 10-15：仅提取用，clean_name 不执行删除 ---
# 10. [EXTRACT] 季集-S01E01格式（含空格变体 S01 E01）
[Ss](\d{1,2})[\s\._-]*[Ee](\d{1,3})

# 13. [EXTRACT] 季集-EP01格式
[Ee][Pp]?[\s\._-]*(\d{1,3})
```

**如何自定义 `[EXTRACT]` 规则**：在 RegexLab 中，于规则行**上方**添加以 `# [EXTRACT]` 开头的注释行。该规则将只用于 `extract_season_episode()` 等提取方法，不会在 `clean_name()` 中执行删除。

`_load_patterns()` 识别逻辑：

```python
for line in raw.splitlines():
    if line.strip().startswith('#'):
        is_extraction_rule = '[EXTRACT]' in line
        continue
    if is_extraction_rule:
        continue  # 跳过，不加入 _filter_patterns
    self._filter_patterns.append(re.compile(line, re.IGNORECASE))
```

#### 层 2：保护性前置提取 (Pre-extraction)

在 `_step_ai_extraction()` 中，**任何正则删除发生之前**，系统先对原始文件名物理提取 Season/Episode 并缓存：

```python
# ── 保护性提取：在任何正则删除之前，先物理提取 S/E ──
_pre_season, _pre_episode = _cleaner.extract_season_episode(raw_filename)
if _pre_season is not None or _pre_episode is not None:
    if _pre_season  is not None and task.get("season")  is None:
        task["season"]  = _pre_season
    if _pre_episode is not None and task.get("episode") is None:
        task["episode"] = _pre_episode

# 此后才执行正则去噪
cleaned_filename = _cleaner.clean_name(raw_filename)
```

#### 层 3：季集锁定护盾 (S/E Lock Shield)

AI 返回结果后，若正则已明确提取到 S/E，强制覆盖 AI 输出，AI 无权修改或丢弃：

```python
if _locked_season is not None:
    ai_result["season"] = _locked_season   # AI 建议被覆盖
if _locked_episode is not None:
    ai_result["episode"] = _locked_episode  # AI 建议被覆盖
```

`locked_season`/`locked_episode` 同样透传至 `ai_identify_media()` 的所有降级分支（JSON 解析失败、语义 FAIL 等），确保任何路径下集号不丢失。

### TV 重复检测三元组修复

当 `type='tv'` 且 `episode_num is None` 时，跳过 `check_media_exists()` 防重逻辑，进入待定状态：

```python
if refined_type == "tv" and episode_num is None:
    pass  # 缺集号，跳过防重，继续归档
elif imdb_id and db.check_media_exists(imdb_id, refined_type, season_num, episode_num):
    # 三元组（imdb_id + season + episode）精确命中 → 才执行防重
    db.mark_task_as_ignored_and_inherit(...)
    return None
```

### 验证结果（测试文件名）

```
输入：Joy of Life 2019 S01 E01 1080p WEB-DL AVC AAC LC-MayiWeb.mp4
清洗：Joy of Life S01 E01 LC   ← 标题保留，技术噪声去除（S/E 不被删除）
S=1  E=1                        ← 正确提取，不再丢失
```

---

## 图表 1：全量刮削终极流水线 (The Ultimate Scrape Pipeline)

> v1.0.0-Atomic 升级版：含保护性提取、锁定护盾、三元组防重熔断。

```mermaid
flowchart TD
    Start(["🚀 触发全量刮削\nPOST /scrape_all"]) --> LockCheck{"物理级并发防重锁\nthreading.Lock blocking=False"}
    LockCheck -->|锁被占用| Reject["⚠️ 拦截并发请求\n直接丢弃（不排队）"]
    Reject --> End1([结束])

    LockCheck -->|获取成功| SecondCheck{"🔒 锁内二次复检\nis_running 状态快照"}
    SecondCheck -->|is_running=True| Release1["🔓 释放锁\n消除竞态窗口"]
    Release1 --> End1

    SecondCheck -->|is_running=False| SetRunning["✅ 写入运行态\nscrape_all_status=True"]
    SetRunning --> APICheck{"检查 TMDB API Key"}
    APICheck -->|缺失| APIFail["❌ set_error() 返回"]
    APIFail --> Finally1["finally:\nis_running=False / 释放锁"]
    Finally1 --> End2([结束])

    APICheck -->|存在| GetTasks["📋 获取待刮削任务\n双表联查 tasks + media_archive"]
    GetTasks --> TaskLoop["🔄 逐个任务\n_process_single_task()"]

    subgraph Orchestrator ["📦 _process_single_task() 编排器"]
        direction TD
        NFO{"[Step 1] _step_nfo_shortcut\n查找本地 NFO"}
        NFO -->|NFO 命中| NFOParse["解析 NFO（三层装甲）\n提取 tmdb/imdb/title/year"]
        NFOParse --> NFOClose["🧷 闭环落库（终态）\nstatus=archived / sub_status=scraped"]

        NFO -->|未命中| SubSave{"极致省流\narchived+缺imdb_id\n有本地字幕?"}
        SubSave -->|有字幕| SaveToken["⚡ 零 Token 省流\nreturn True, False"]

        SubSave -->|无字幕| PreExtract["🛡️ [原子协议-层2] 保护性前置提取\nextract_season_episode(raw_filename)\n→ locked_season / locked_episode 缓存"]
        PreExtract --> CleanStep["🧹 RegexLab 物理正则去噪\nclean_name(raw_filename)\n[EXTRACT] 规则隔离，S/E 不被删除"]
        CleanStep --> AIStep["🧠 [Step 2] AI 语义分析\nai_identify_media(\n  cleaned_name,\n  locked_season=locked_season,\n  locked_episode=locked_episode\n)"]
        AIStep --> LockShield["🔒 [原子协议-层3] 季集锁定护盾\nAI 输出被正则锁定值强制覆盖\ntask['season'] / task['episode'] 同步"]
        LockShield --> YearMirror["⚖️ YEAR_MIRROR 年份裁决\nfilename_year vs knowledge_year\n冲突→year 置空（模糊搜索）"]
        YearMirror --> TMDBTier["🔍 TMDB 三梯队降级\nTier-1: Title+Year\nTier-2: Title only\nTier-3: Truncated Title"]
        TMDBTier --> DupCheck{"🛡️ [Step 3] IMDb+S+E 三元组\n防重熔断\ntype=tv & episode=None → 跳过"}
        DupCheck -->|三元组命中| IgnoreClose["🧷 闭环落库（终态）\nmark_task_as_ignored_and_inherit\n原子继承海报"]
        DupCheck -->|未重复| ArchStep["[Step 4] _step_archive_and_metadata\n就地补录 or 归档全链路"]
        ArchStep --> SubCheck["🎁 字幕白嫖\n_check_local_subtitles\n零 API 消耗"]
    end

    TaskLoop --> NFO
    NFOClose --> LoopEnd{"所有任务完成?"}
    SaveToken --> LoopEnd
    SubCheck --> LoopEnd
    IgnoreClose --> LoopEnd

    LoopEnd -->|否| TaskLoop
    LoopEnd -->|是| UpdateStats["📊 finish_run()\n写入统计"]
    UpdateStats --> Finally2["finally:\nis_running=False / 释放锁"]
    Finally2 --> End3(["✅ 全量刮削完成"])
```

---

## 图表 2：核级重构完整生命周期（时序图）

**场景**：用户触发「核级重构 (Nuclear Rebuild)」到后端完成写库的完整生命周期。

```mermaid
sequenceDiagram
    participant User as 👤 用户
    participant Dialog as 🎨 RebuildDialog.tsx
    participant API as 🌐 api.ts
    participant Route as 🚀 FastAPI Route
    participant Engine as ⚙️ manual_rebuild()
    participant Repo as 💾 TaskRepo
    participant DB as 🗄️ SQLite

    User->>Dialog: 1. 点击「执行核级重构」按钮
    Dialog->>Dialog: 2. 验证 tmdb_id 非空
    Dialog->>Dialog: 3. 构建请求参数
    Dialog->>API: 4. POST /tasks/manual_rebuild
    API->>Route: 5. 发送 HTTP 请求
    Route->>Engine: 6. 调用 manual_rebuild(body)
    Engine->>Engine: 7. 读取任务记录（热表/冷表）
    Engine->>Engine: 8. 校验 target_path 有效性
    Engine->>Engine: 9. 初始化 TMDB 适配器
    Engine->>Engine: 10. 金标准防重预检（NFO IMDb ID 比对）
    Engine->>Engine: 11. 核级清理 _nuclear_clean_directory
    Engine->>Engine: 12. 视频文件重命名
    Engine->>Engine: 13. 文件夹土木工程
    Engine->>Repo: 14. update_any_task_metadata()
    Repo->>DB: 15. UPDATE tasks/media_archive
    DB-->>Repo: 16. 返回更新结果
    Engine->>Engine: 17. NFO 生成与写入
    Engine->>Engine: 18. 海报下载与存储
    Engine->>Engine: 19. 字幕检测（本地白嫖）
    Engine-->>Route: 20. 返回 {success, rebuilt: {nfo, poster, subtitle, nuclear}}
    Route-->>API: 21. HTTP 200 OK
    API-->>Dialog: 22. 解析响应
    Dialog-->>User: 23. 显示成功提示 + 刷新列表
```

---

## 图表 3：单飞高并发防御阵列（Singleflight Concurrency Shield）

**场景**：前端 500 个并发请求 `/public/image` 拉取海报，TTL 缓存失效时仅允许 1 个协程访问 DB，其余走 stale-while-revalidate。

```mermaid
sequenceDiagram
    autonumber
    participant FE as Frontend (500 并发)
    participant API as FastAPI /public/image
    participant Cache as TTL Cache
    participant Lock as asyncio.Lock (singleflight)
    participant DB as SQLite
    participant FS as FileResponse

    FE->>API: GET /public/image?path=...
    API->>Cache: 读取缓存（检查 TTL）
    alt TTL 命中（fresh）
        Cache-->>API: 返回 allowed_paths（fresh）
        API->>FS: Path 校验通过 → FileResponse
        FS-->>FE: 200 image bytes
    else TTL 过期（stale）
        Cache-->>API: 返回 stale 数据
        par 499 个请求（stale-while-revalidate）
            API->>FS: 用旧 allowlist 先放行
            FS-->>FE: 200 image bytes（低延迟）
        and 1 个请求触发 Singleflight
            API->>Lock: acquire()
            Lock-->>API: ✅ 获得锁（唯一放行）
            API->>DB: to_thread.run_sync 查询 managed_paths
            DB-->>API: 返回最新 allowed_paths
            API->>Cache: 更新缓存 + 刷新 TTL
            API->>Lock: release()
            API->>FS: FileResponse
            FS-->>FE: 200 image bytes
        end
    end
```

---

## 图表 4：前端量子态神经中枢（Neural Link State Sync）

```mermaid
graph LR
    subgraph Backend[后端信号源]
        S1[/GET /system/stats/]
        S2[/GET /tasks/*_status/]
        S3[/GET /tasks/list/]
    end

    subgraph Hub[前端量子态枢纽]
        H1[useNeuralLinkStatus\nSingleton Store]
        H2[Polling Loop\n单例轮询 + AbortController]
        H3[emit() 派发快照\n订阅者模式]
    end

    subgraph UI[订阅者（同频渲染）]
        U1[AiSidebar]
        U2[SystemMonitor]
        U3[MiniLog]
    end

    subgraph I18N[i18n 渲染层]
        T1[useLanguage().t]
        T2[动态键保护红线\nstatus_ / sub_status_ / ui_]
    end

    S1 --> H2
    S2 --> H2
    S3 --> H2
    H2 --> H1
    H1 --> H3
    H3 --> U1
    H3 --> U2
    H3 --> U3
    U1 --> T1
    U2 --> T1
    U3 --> T1
    T2 --> T1

    classDef src fill:#16213e,stroke:#00e6f6,color:#00e6f6;
    classDef hub fill:#0f3460,stroke:#00ff9f,color:#00ff9f;
    classDef ui fill:#1a1a2e,stroke:#f5e642,color:#f5e642;
    classDef i18n fill:#3d0000,stroke:#ff3c5a,color:#ff3c5a;
    class S1,S2,S3 src;
    class H1,H2,H3 hub;
    class U1,U2,U3 ui;
    class T1,T2 i18n;
```

---

## 图表 5：冷热双表数据流转（状态图）

```mermaid
stateDiagram-v2
    [*] --> Pending: 文件下载完成 / 扫描入库
    Pending --> Processing: 触发全量刮削
    Processing --> NFOShortcut: NFO 短路拦截?
    NFOShortcut --> Archived: ✅ 是，直接归档
    NFOShortcut --> PreExtract: ❌ 否
    PreExtract --> AIExtract: 🛡️ 保护性提取 S/E\n→ RegexLab 去噪\n→ AI 分析（携带 locked S/E）
    AIExtract --> TMDBSearch: TMDB 三梯队搜索
    TMDBSearch --> DupDetect: 获得 IMDb ID
    DupDetect --> Ignored: 🚨 三元组防重命中\n(imdb+season+episode)
    DupDetect --> InPlace: 文件在 library?
    InPlace --> Archived: ✅ 是，就地补录
    InPlace --> Archive: ❌ 否，归档全链路
    Archive --> Archived: SmartLink + 元数据写入
    Archived --> SubtitleCheck: 字幕白嫖检测
    SubtitleCheck --> SubSuccess: ✅ 发现本地字幕
    SubtitleCheck --> SubPending: ❌ 无本地字幕
    SubSuccess --> ColdTable: ❄️ 流转冷表 media_archive
    SubPending --> ColdTable
    Ignored --> IgnoredFinal: 🚨 status=ignored
    Processing --> Failed: 🔴 处理失败
    Failed --> Processing: 用户手动重试
    ColdTable --> [*]: ✅ 最终状态，入库完成
    IgnoredFinal --> [*]: 🚨 最终状态，重复文件标记
```

---

## 图表 6：三级物理删除流

**场景**：用户点击 `MediaTable` 中任意层级的红色 Trash2 删除按钮。

```mermaid
flowchart TD
    User(["👤 用户点击 Trash2"]) --> Level{节点层级}
    Level -->|电影 / 单集 level=2| SingleDel["🗑️ onDelete(task.id)"]
    Level -->|季节点 level=1| SeasonDel["📦 聚合该季所有 episode.id\n→ onDeleteBatch(seasonIds)"]
    Level -->|剧集根 level=0| TVDel["📦 聚合全剧所有 episode.id\n→ onDeleteBatch(allEpisodeIds)"]
    SingleDel --> ConfirmSingle["confirm() 弹窗"]
    SeasonDel --> ConfirmBatch["confirm() 弹窗"]
    TVDel --> ConfirmBatch
    ConfirmSingle -->|确认| APIDelete["DELETE /tasks/{task_id}"]
    ConfirmBatch -->|确认| APIBatch["POST /tasks/delete_batch"]
    APIDelete --> Reload["await loadTasks() 刷新列表"]
    APIBatch --> Reload
    ConfirmSingle -->|取消| Cancel([🚫 无操作])
    ConfirmBatch -->|取消| Cancel
```

| 层级 | 节点 | API 端点 | 备注 |
|------|------|---------|------|
| level=0（电影） | UniversalMediaRow | `DELETE /tasks/{id}` | 单条删除 |
| level=0（剧集根） | UniversalMediaRow | `POST /tasks/delete_batch` | 聚合全剧所有集 ID |
| level=1（季节点） | UniversalMediaRow | `POST /tasks/delete_batch` | 聚合该季所有集 ID |
| level=2（单集） | UniversalMediaRow | `DELETE /tasks/{id}` | 单条删除 |

> 批量删除仅删除数据库记录（`tasks` + `media_archive` 双表），**不删除物理文件**。

---

## 🔄 关键业务流程总结表

| 流程 | 触发条件 | 关键操作 | 输出状态 |
|------|---------|---------|----------|
| **NFO 短路** | 本地存在 NFO | 解析 NFO → 提取元数据 → 跳过 AI/TMDB | `(True, False)` |
| **极致省流** | archived + 缺 imdb_id + 有字幕 | 跳过全流程，零 Token | `(True, False)` |
| **原子提取** | 所有非 NFO 短路任务 | 保护性提取 S/E → RegexLab 去噪 → AI 分析（携带 locked S/E） | locked_season/episode |
| **AI 提炼** | 原子提取完成 | `_step_ai_extraction`：YEAR_MIRROR 镜像校验 | refined_query/year/type |
| **TMDB 搜索** | AI 提炼完成 | 三梯队降级搜索 → 精确/宽松匹配 | tmdb_data dict |
| **三元组防重** | 获得 IMDb ID | `check_media_exists(imdb_id, type, season, episode)` | `ignored` 或继续 |
| **就地补录** | 文件在 library 路径 | 补充 NFO/海报，不移动文件 | `archived` |
| **归档全链路** | 文件在下载目录 | SmartLink → NFO → 海报 → Fanart | `archived` |
| **字幕白嫖** | 归档完成 | `_check_local_subtitles` → 若有标记 scraped | `scraped`/`pending` |

---

## 📌 RegexLab `filename_clean_regex` 配置规范

### `[EXTRACT]` 标记使用方法

`filename_clean_regex` 中每条规则的行为由其**上方注释行**控制：

| 注释格式 | 行为 | 适用场景 |
|---------|------|----------|
| `# 普通注释` | **CLEAN** — 参与 `clean_name()` 删除 | 分辨率、编码、语言等噪声标签 |
| `# ... [EXTRACT] ...` | **EXTRACT** — 仅参与结构化提取，不删除 | 季集号、集数格式（S01E01 等） |

**示例**：

```
# 1. 分辨率标签过滤                          ← 普通规则，clean_name 会删除
\b(2160p|1080p|720p)\b

# 10. [EXTRACT] 季集-S01E01格式（含空格）    ← EXTRACT 规则，clean_name 不删除
[Ss](\d{1,2})[\s\._-]*[Ee](\d{1,3})
```

**当前默认规则分类**：

| 规则编号 | 类型 | 描述 |
|---------|------|------|
| 1–9 | CLEAN | 分辨率、编码、方括号标签、广告词、语言标签、制作组、年份 |
| 10–15 | **EXTRACT** | 所有季集号格式（S01E01、Season格式、1x01、EP01、中文格式、动漫格式）|

---

*Neon-Crate | DEV-ARCH-001 | 2026-03-18 | v1.0.0-Atomic*
