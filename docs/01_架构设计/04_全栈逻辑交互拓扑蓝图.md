# DEV-ARCH-001 全栈逻辑交互拓扑蓝图

**文档编号**：DEV-ARCH-001  
**日期**：2026-03-16  
**状态**：✅ 架构绘图完成（含可视化图表）

---

## 📋 文档概述

本文档基于全栈源码级注释（Phase 1-1.6），使用 Mermaid.js 绘制多张核心业务流程图，展示 Neon-Crate 系统的完整交互拓扑。

---

## 图表 1：核级重构完整生命周期（时序图）

**场景**：用户在前台触发「核级重构 (Nuclear Rebuild)」直到后端完成并写入数据库的完整生命周期。

```mermaid
sequenceDiagram
    participant User as 👤 用户
    participant Dialog as 🎨 RebuildDialog.tsx
    participant API as 🌐 api.ts
    participant Route as 🚀 FastAPI Route
    participant Engine as ⚙️ manual_rebuild()
    participant Repo as 💾 TaskRepo
    participant DB as 🗄️ SQLite

    User->>Dialog: 1. 点击「执行核级重构」按钮
    Dialog->>Dialog: 2. 验证 tmdb_id 非空
    Dialog->>Dialog: 3. 构建请求参数<br/>(task_id, is_archive, nuclear_reset=true)
    Dialog->>API: 4. POST /tasks/manual_rebuild
    
    API->>API: 5. 合并默认参数<br/>(is_archive=true, refix_nfo=true, etc.)
    API->>Route: 6. 发送 HTTP 请求
    
    Route->>Engine: 7. 调用 manual_rebuild(body)
    Engine->>Engine: 8. 读取任务记录<br/>(根据 is_archive 选择热表/冷表)
    Engine->>Engine: 9. 校验 target_path 有效性
    Engine->>Engine: 10. 初始化 TMDB 适配器
    
    Engine->>Engine: 11. 金标准防重预检<br/>(检查本地 NFO 中的 IMDb ID)
    Engine->>Engine: 12. 核级清理<br/>(_nuclear_clean_directory)
    Engine->>Engine: 13. 视频文件重命名
    Engine->>Engine: 14. 文件夹土木工程
    
    Engine->>Repo: 15. 调用 update_any_task_metadata()
    Repo->>DB: 16. UPDATE tasks/media_archive<br/>(根据 is_archive 选择表)
    DB-->>Repo: 17. 返回更新结果
    
    Engine->>Repo: 18. 调用 update_task_status()
    Repo->>DB: 19. UPDATE tasks SET status='archived'<br/>(触发归档流程)
    DB-->>Repo: 20. 返回更新结果
    
    Engine->>Engine: 21. NFO 生成与写入
    Engine->>Engine: 22. 海报下载与存储
    Engine->>Engine: 23. 字幕检测（本地白嫖）
    
    Engine-->>Route: 24. 返回重建结果<br/>{success, rebuilt: {nfo, poster, subtitle, nuclear}}
    Route-->>API: 25. HTTP 200 OK
    API-->>Dialog: 26. 解析响应
    Dialog-->>User: 27. 显示成功提示 + 刷新列表
```

---

## 图表 2：全量刮削引擎核心流程（决策树）

**场景**：`perform_scrape_all_task_sync` + `_process_single_task` 编排器的内部运转机制（V2.0 流水线编排器架构）。

```mermaid
flowchart TD
    Start([🚀 开始全量刮削]) --> LockCheck{获取防重锁\nblockinng=False}
    LockCheck -->|锁被占用| Reject["⚠️ 拦截并发请求\n直接返回"]
    Reject --> End1([结束])

    LockCheck -->|获取成功| SecondCheck{🔒 锁内二次\n状态复检\n状态快照(dict)}
    SecondCheck -->|is_running=True| ReleaseLock1["🔓 主动释放锁\n退出（消除竞态窗口）"]
    ReleaseLock1 --> End1

    SecondCheck -->|is_running=False| SetRunning["✅ 写入运行态\nscrape_all_status['is_running']=True\n并清空 error"]
    SetRunning --> APICheck{检查 TMDB\nAPI Key}
    APICheck -->|缺失| APIFail["❌ set_error()\n返回"]
    APIFail --> Finally1["finally:\n写入 is_running=False\n释放锁"]
    Finally1 --> End2([结束])

    APICheck -->|存在| GetTasks["📋 获取待刮削任务列表\n双表联查 tasks + media_archive"]
    GetTasks --> TaskLoop["🔄 逐个处理任务\n_process_single_task()"]

    subgraph Orchestrator ["📦 编排器 _process_single_task()"
    ]
        direction TD
        Step1{_step_nfo_shortcut\n查找本地 NFO}
        Step1 -->|NFO 命中| NFOSuccess["✅ 解析 NFO（三层装甲）\n提取 tmdb/imdb/title/year"]
        NFOSuccess --> NFOClose["🧷 状态机闭环落库（终态）\nstatus=archived\nsub_status=success\nis_active=1\nreturn True, False"]
        Step1 -->|未命中| SubSaving{极致省流\narchived+缺imdb_id\n有本地字幕?}
        SubSaving -->|有字幕| SaveToken["⚡ 跳过刮削\n零 Token 消耗\nreturn True, False"]
        SubSaving -->|无字幕| Step2["🧠 _step_ai_extraction\n正则去噪→AI识别→路径权威\n→Year Mirror 年份护盾（冲突置空）"]
        Step2 --> TMDBTier["🔍 TMDB 三梯队降级\nTier-1 Title+Year\nTier-2 Title only\nTier-3 Truncated Title"]
        TMDBTier --> Step3["🧲 _step_tmdb_search_and_dup_check\n精确匹配→IMDb 防重熔断"]
        Step3 -->|None（失败）| ReturnFail["return False, True"]
        Step3 -->|物理副本命中| IgnoreClose["🧷 状态机闭环落库（终态）\nstatus=ignored\n继承存量海报\nsub_status=success\nis_active=1\nreturn False, True"]
        Step3 -->|tmdb_data| Step4["🏠 _step_archive_and_metadata\n就地补录 / 归档全链路\nSmartLink→NFO→海报→Fanart"]
        Step4 -->|target_path=None| ReturnFail
        Step4 -->|成功| SubCheck["🎁 字幕白嫖\n_check_local_subtitles\nreturn True, False"]
    end

    TaskLoop --> Step1
    NFOClose --> LoopEnd{所有任务\n处理完?}
    SaveToken --> LoopEnd
    ReturnFail --> LoopEnd
    IgnoreClose --> LoopEnd
    SubCheck --> LoopEnd

    LoopEnd -->|否| TaskLoop
    LoopEnd -->|是| UpdateStats["📊 finish_run(processed)\n原子写入统计"]
    UpdateStats --> Finally2["finally:\n写入 is_running=False\n释放锁"]
    Finally2 --> End3([✅ 全量刮削完成])
```

---

## 图表 3：冷热双表数据流转（状态图）

**场景**：一个媒体文件从下载完成到最终入库的状态流转。

```mermaid
stateDiagram-v2
    [*] --> Pending: 文件下载完成<br/>扫描入库
    
    Pending --> Pending: 等待刮削<br/>status='pending'
    
    Pending --> Processing: 触发全量刮削
    
    Processing --> NFOShortcut: NFO 短路拦截?
    NFOShortcut --> Archived: ✅ 是<br/>直接归档
    NFOShortcut --> AIExtract: ❌ 否<br/>继续流程
    
    AIExtract --> TMDBSearch: AI 提炼完成
    
    TMDBSearch --> DupDetect: TMDB 搜索完成
    
    DupDetect --> Ignored: 🚨 IMDb 防重<br/>检测到重复
    
    DupDetect --> InPlace: 文件在 library?
    InPlace --> Archived: ✅ 是<br/>补充元数据
    
    DupDetect --> Archive: ❌ 否<br/>归档全链路
    Archive --> Archived: 移动文件<br/>写入元数据
    
    Archived --> SubtitleCheck: 字幕白嫖<br/>检测本地字幕
    SubtitleCheck --> SubSuccess: ✅ 发现本地字幕
    SubtitleCheck --> SubPending: ❌ 无本地字幕
    
    SubSuccess --> HotTable: 🔥 热表 tasks
    SubPending --> HotTable: 🔥 热表 tasks
    
    HotTable --> ColdArchive: 触发冷表归档
    
    ColdArchive --> ColdTable: ❄️ 冷表 media_archive
    
    Ignored --> IgnoredState: 🚨 热表 tasks<br/>status='ignored'
    
    Processing --> Failed: 🔴 处理失败
    Failed --> Retry: 用户手动重试
    Retry --> Processing
    
    ColdTable --> [*]: ✅ 最终状态<br/>媒体库入库完成
    IgnoredState --> [*]: 🚨 最终状态<br/>重复文件标记
```

---

## 📊 核心数据契约与可视化

### 前后端数据传递流程

```mermaid
graph LR
    A["🎨 RebuildDialog.tsx<br/>前端请求体"] -->|POST /tasks/manual_rebuild| B["🌐 api.ts<br/>HTTP 传输"]
    B -->|HTTP 200 OK| C["🚀 FastAPI Route<br/>后端响应体"]
    
    style A fill:#00e6f6,stroke:#00ff9f,color:#000
    style B fill:#f5e642,stroke:#ff3c5a,color:#000
    style C fill:#00ff9f,stroke:#00e6f6,color:#000
```

### 数据库双表架构

```mermaid
graph TB
    subgraph Hot["🔥 热表 (tasks)"]
        H1["id (PK)"]
        H2["status: pending/archived/failed/ignored"]
        H3["is_archive: 0"]
        H4["target_path"]
    end
    
    subgraph Cold["❄️ 冷表 (media_archive)"]
        C1["original_task_id (PK)"]
        C2["status: archived (固定)"]
        C3["is_archive: 1"]
        C4["target_path"]
    end
    
    Hot -.->|archive_task()| Cold
    
    style Hot fill:#ff3c5a,stroke:#00e6f6,color:#fff
    style Cold fill:#00e6f6,stroke:#00ff9f,color:#000
```

### 关键业务流程

```mermaid
graph TD
    Start([文件下载完成]) --> NFO{_step_nfo_shortcut\n本地存在 NFO?}

    NFO -->|是| NFOShortcut["✅ NFO 短路\n解析 NFO → 提取元数据\nreturn True, False"]
    NFO -->|否| SubSave{极致省流\narchived+缺imdb_id\n有本地字幕?}
    SubSave -->|有| SaveToken["⚡ 零 Token 省流\nreturn True, False"]
    SubSave -->|无| AIExtract["🧠 _step_ai_extraction\n正则清洗 → AI 分析\n路径权威 → YEAR_MIRROR 镜像校验"]

    AIExtract --> TMDBSearch["🔍 _step_tmdb_search_and_dup_check\nTMDB 搜索 → 精确匹配 → IMDb 防重熔断"]
    TMDBSearch --> DupCheck{IMDb ID\n防重熔断}

    DupCheck -->|重复| Ignored["🚨 mark_task_as_ignored_and_inherit\n原子继承海报\nreturn False, True"]
    DupCheck -->|不重复| ArchStep["🏠 _step_archive_and_metadata\n就地补录 or 归档全链路"]

    ArchStep --> LibCheck{文件在 library?}
    LibCheck -->|是| InPlace["🏥 就地补录\nNFO/海报/Fanart 写入\n不移动文件"]
    LibCheck -->|否| Archive["📦 归档全链路\nSmartLink → NFO → 海报 → Fanart"]

    NFOShortcut --> SubCheck["🎁 字幕白嫖\n_check_local_subtitles"]
    SaveToken --> SubCheck
    InPlace --> SubCheck
    Archive --> SubCheck

    SubCheck -->|有| End1([✅ success])
    SubCheck -->|无| End2([⏳ pending 等待字幕搜索])
    Ignored --> End3([🚨 ignored])

    style NFOShortcut fill:#00ff9f,stroke:#00e6f6,color:#000
    style AIExtract fill:#f5e642,stroke:#ff3c5a,color:#000
    style TMDBSearch fill:#00e6f6,stroke:#00ff9f,color:#000
    style Ignored fill:#ff3c5a,stroke:#f5e642,color:#fff
    style InPlace fill:#00e6f6,stroke:#00ff9f,color:#000
    style Archive fill:#00ff9f,stroke:#00e6f6,color:#000
    style SaveToken fill:#b8ff9f,stroke:#00e6f6,color:#000
```

---

## 🔄 关键业务流程总结表

| 流程 | 触发条件 | 关键操作 | 输出状态 |
|------|---------|---------|---------|
| **NFO 短路** | 本地存在 NFO 文件 | `_step_nfo_shortcut`：解析 NFO → 提取元数据 → 跳过 AI/TMDB | `(True, False)` |
| **极致省流** | archived + 缺 imdb_id + 有本地字幕 | 跳过全流程，零 Token | `(True, False)` |
| **AI 提炼** | NFO/省流均未命中 | `_step_ai_extraction`：正则清洗 → AI 分析 → 路径权威 → YEAR_MIRROR 镜像校验 | 输出 refined_query/final_year/type |
| **TMDB 搜索** | AI 提炼完成 | `_step_tmdb_search_and_dup_check`：调用 TMDB API → 精确/宽松匹配 | 输出 tmdb_data dict |
| **防重拦截** | 获得 IMDb ID | `mark_task_as_ignored_and_inherit`（原子操作）→ 继承海报 | `(False, True)` |
| **就地补录** | 文件在 library 路径 | `_step_archive_and_metadata`：补充 NFO/海报 → 不移动文件 | archived |
| **归档全链路** | 文件在下载目录 | `_step_archive_and_metadata`：SmartLink → NFO → 海报 → Fanart | archived |
| **字幕白嫖** | 归档完成 | `_check_local_subtitles`：检测本地字幕 → 若有标记 success | success/pending |

---

## 图表 N：媒体库三级物理删除流（V16 新增）

**场景**：用户在 `MediaTable` 中点击任意层级的红色 Trash2 删除按钮，触发对应级别的物理删除流程。

```mermaid
flowchart TD
    User([👤 用户点击 Trash2]) --> Level{节点层级}

    Level -->|电影 / 单集 level=2| SingleDel["🗑️ onDelete(task.id)"]
    Level -->|季节点 level=1| SeasonDel["📦 聚合该季所有 episode.id\n→ onDeleteBatch(seasonIds)"]
    Level -->|剧集根 level=0| TVDel["📦 聚合全剧所有 episode.id\n→ onDeleteBatch(allEpisodeIds)"]

    SingleDel --> ConfirmSingle["confirm() 弹窗\n（MediaWall.handleDelete）"]
    SeasonDel --> ConfirmBatch["confirm() 弹窗\n（MediaWall.handleDeleteBatch）"]
    TVDel --> ConfirmBatch

    ConfirmSingle -->|确认| APIDelete["DELETE /tasks/{task_id}\napi.deleteTask(id)"]
    ConfirmBatch -->|确认| APIBatch["POST /tasks/delete_batch\napi.deleteBatchTasks(ids[])"]

    APIDelete --> Reload["await loadTasks()\n刷新列表"]
    APIBatch --> Reload

    ConfirmSingle -->|取消| Cancel([🚫 无操作])
    ConfirmBatch -->|取消| Cancel

    style SingleDel fill:#00e6f6,stroke:#00ff9f,color:#000
    style SeasonDel fill:#f5e642,stroke:#ff3c5a,color:#000
    style TVDel fill:#ff3c5a,stroke:#f5e642,color:#fff
    style APIDelete fill:#00e6f6,stroke:#00ff9f,color:#000
    style APIBatch fill:#ff3c5a,stroke:#f5e642,color:#fff
```

**三级删除能力说明：**

| 层级 | 节点 | 前端逻辑 | API 端点 | 备注 |
|------|------|---------|---------|------|
| level=0（电影） | `UniversalMediaRow` | `onDelete(task.id)` | `DELETE /tasks/{id}` | 单条删除 |
| level=0（剧集根） | `UniversalMediaRow` | `onDeleteBatch(allEpisodeIds)` | `POST /tasks/delete_batch` | 聚合全剧所有集 ID |
| level=1（季节点） | `UniversalMediaRow` | `onDeleteBatch(seasonIds)` | `POST /tasks/delete_batch` | 聚合该季所有集 ID |
| level=2（单集） | `UniversalMediaRow` | `onDelete(ep.id)` | `DELETE /tasks/{id}` | 单条删除 |

> **架构要点**：删除按钮在所有层级**常驻显示**（红色 Trash2），无需展开即可操作。  
> 批量删除仅删除数据库记录（`tasks` + `media_archive` 双表），**不删除物理文件**。

---

*Neon-Crate | DEV-ARCH-001 | 2026-03-17 | 含可视化图表*
