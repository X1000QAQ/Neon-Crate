# 无损核重构防爆护盾方案验证报告

**文档编号**：DEV-VERIFY-020  
**日期**：2026-03-16  
**状态**：✅ 方案验证完成，待点火

---

## 一、机长纠偏确认

机长指出的「先杀后审悖论」**完全成立**。原侦察报告（DEV-RECON-018）将防重校验放在 Step 3.8，即 `_nuclear_clean_directory` 之后。但核级清理（Step 3.5 第 1186 行）会把 `tvshow.nfo` 和 `poster.jpg` 一并删除，导致后续解析永远找不到 NFO 文件，`skip_metadata` 永远为 `False`，防重逻辑完全失效。

**正确执行顺序必须是：**
```
读取 TMDB 详情 (Step 3.x)
  ↓
【NEW】Step 3.4: IMDb 金标准预检（在清理之前）
  ↓
Step 3.5: _nuclear_clean_directory（护盾激活时保留 NFO/poster）
  ↓
Step 3.x: 视频重命名
  ↓
Step 3.x: 文件夹土木工程
  ↓
Step 3.x: DB 同步
  ↓
Step 4: refix_nfo（护盾激活时直接沿用）
  ↓
Step 5: refix_poster（护盾激活时直接沿用）
```

---

## 二、代码现状精准定位

| 位置 | 行号 | 内容 |
|------|------|------|
| `_nuclear_clean_directory` 定义 | 943 | 函数签名：`def _nuclear_clean_directory(metadata_dir, library_root, video_exts=None)` |
| Step 3.5 核级重置协议 | 1160 | `if body.nuclear_reset and new_tmdb_id:` |
| `_nuclear_clean_directory` 调用 | 1186 | `nuclear_result = _nuclear_clean_directory(metadata_dir, library_root, video_exts=_db_video_exts)` |
| Step 4 refix_nfo | 1282 | `if body.refix_nfo and new_tmdb_id:` |
| Step 5 refix_poster | 1308 | `if body.refix_poster and new_tmdb_id:` |

---

## 三、三阶段修改方案

### Phase 1: 升级 `_nuclear_clean_directory` 函数签名

**修改位置**：第 943 行函数定义

```python
# 修改前
def _nuclear_clean_directory(metadata_dir: str, library_root: str, video_exts: frozenset = None) -> dict:

# 修改后
def _nuclear_clean_directory(metadata_dir: str, library_root: str, video_exts: frozenset = None, protect_metadata: bool = False) -> dict:
```

**修改位置**：函数内部 `for f in resolved_meta.iterdir()` 循环，在视频保留判断之后追加护盾分支：

```python
# 修改前（循环体末尾）
        if f.suffix.lower() in _video_exts:
            kept_videos.append(str(f))
            logger.info(f"[NUCLEAR] 保留视频本体: {f.name}")
        else:
            f.unlink()
            deleted.append(str(f))
            logger.info(f"[NUCLEAR] 已删除: {f.name}")

# 修改后
        _protected_names = {"tvshow.nfo", "movie.nfo", "poster.jpg", "poster.png", "poster.webp",
                            "fanart.jpg", "fanart.png", "fanart.webp"}
        if f.suffix.lower() in _video_exts:
            kept_videos.append(str(f))
            logger.info(f"[NUCLEAR] 保留视频本体: {f.name}")
        elif protect_metadata and f.name.lower() in _protected_names:
            logger.info(f"[NUCLEAR] 金标准护盾生效，保留元数据: {f.name}")
            continue
        else:
            f.unlink()
            deleted.append(str(f))
            logger.info(f"[NUCLEAR] 已删除: {f.name}")
```

> ⚠️ `_protected_names` 应定义在 `if f.suffix.lower()` 判断之前（循环外更优），但为减少变更范围，置于循环内亦可。

---

### Phase 2: 在 Step 3.5 之前插入 Step 3.4 金标准预检

**插入位置**：第 1160 行 `# ── 3.5 核级重置协议` 注释之前

```python
# ── 3.4 金标准元数据防重预检 (Pre-emptive IMDb Validation) ────────
# 必须在 _nuclear_clean_directory 之前执行，否则 NFO 已被删除无法读取
skip_metadata = False
if new_imdb_id:
    check_dir = metadata_dir
    if body.media_type == "tv" and re.match(
        r'^(Season|S)\s*\d+$|^Specials$',
        os.path.basename(check_dir), re.IGNORECASE
    ):
        check_dir = os.path.dirname(check_dir)
    nfo_filename = "tvshow.nfo" if body.media_type == "tv" else "movie.nfo"
    nfo_path_check = Path(check_dir) / nfo_filename
    if nfo_path_check.exists():
        try:
            import xml.etree.ElementTree as ET
            _tree = ET.parse(nfo_path_check)
            _root = _tree.getroot()
            _existing_imdb = (_root.findtext("imdbid") or "").strip()
            if not _existing_imdb:
                for _uid in _root.findall("uniqueid"):
                    if _uid.get("type") == "imdb":
                        _existing_imdb = (_uid.text or "").strip()
                        break
            if _existing_imdb and _existing_imdb == new_imdb_id.strip():
                skip_metadata = True
                logger.info(f"[REBUILD] 金标准校验通过: 本地已存在 IMDb ({new_imdb_id})，开启防爆护盾")
        except Exception as _e:
            logger.warning(f"[REBUILD] NFO 解析失败，防爆护盾离线: {_e}")
```

---

### Phase 3: 护盾传递与旁路

**3a — 修改 `_nuclear_clean_directory` 调用（第 1186 行）**：
```python
# 修改前
nuclear_result = _nuclear_clean_directory(metadata_dir, library_root, video_exts=_db_video_exts)

# 修改后
nuclear_result = _nuclear_clean_directory(metadata_dir, library_root, video_exts=_db_video_exts, protect_metadata=skip_metadata)
```

**3b — 修改 Step 4 refix_nfo（第 1282 行）**：
```python
# 修改前
    if body.refix_nfo and new_tmdb_id:
        if not body.nuclear_reset:
            ...
        nfo_filename = ...
        nfo_path = ...
        ok = meta_manager.generate_nfo(...)
        rebuilt["nfo"] = ok

# 修改后
    if body.refix_nfo and new_tmdb_id:
        if skip_metadata:
            rebuilt["nfo"] = True
            logger.info("[REBUILD] 金标准护盾：沿用现有 NFO，跳过生成")
        else:
            if not body.nuclear_reset:
                ...
            nfo_filename = ...
            nfo_path = ...
            ok = meta_manager.generate_nfo(...)
            rebuilt["nfo"] = ok
```

**3c — 修改 Step 5 refix_poster（第 1308 行）**：
```python
# 修改前
    if body.refix_poster and new_tmdb_id:
        for _pname in ["poster.jpg", ...]:
            _pp.unlink()
        local_poster = meta_manager.download_poster(...)
        rebuilt["poster"] = bool(local_poster)
        # fanart 下载...
    else:
        local_poster = None

# 修改后
    if body.refix_poster and new_tmdb_id:
        if skip_metadata:
            # 护盾激活：扫描已有海报文件，赋给 local_poster 供 DB 更新使用
            local_poster = None
            for _pname in ["poster.jpg", "poster.png", "poster.webp"]:
                _pp = Path(metadata_dir) / _pname
                if _pp.exists():
                    local_poster = str(_pp)
                    break
            rebuilt["poster"] = True
            logger.info(f"[REBUILD] 金标准护盾：沿用现有海报，跳过下载: {local_poster}")
        else:
            for _pname in ["poster.jpg", ...]:
                _pp.unlink()
            local_poster = meta_manager.download_poster(...)
            rebuilt["poster"] = bool(local_poster)
            # fanart 下载...
    else:
        local_poster = None
```

---

## 四、完整时序图（修复后）

```
同一 TV 剧集，仅修改某集 (S01E03)
  ↓
Step 3.4: 读取 /剧集根/tvshow.nfo → imdbid=tt1190634
          new_imdb_id = tt1190634
          完全匹配 → skip_metadata = True ✅ 防爆护盾激活
  ↓
Step 3.5: _nuclear_clean_directory(..., protect_metadata=True)
          - S01E01.mkv → 保留（视频白名单）
          - tvshow.nfo → 保留（护盾）✅
          - poster.jpg → 保留（护盾）✅
          - fanart.jpg → 保留（护盾）✅
          - *.srt 等   → 删除（非视频非护盾）
  ↓
Step 2:   _rename_video_file() → 重命名 S01E03
  ↓
Step 2.5: 文件夹土木工程（TV 根目录改名 + Season 搬运）
  ↓
Step 3:   DB target_path 同步
  ↓
Step 4:   skip_metadata=True → rebuilt["nfo"] = True（跳过）✅ 零网络请求
  ↓
Step 5:   skip_metadata=True → 扫描 poster.jpg → rebuilt["poster"] = True（跳过）✅ 零网络请求
  ↓
Step 6:   DB 元数据全字段同步

结果：100% 带宽零损耗，仅视频重命名 + DB 同步 ✅
```

---

## 五、边界情况分析

| 场景 | `skip_metadata` | 结果 |
|------|----------------|------|
| 本地 NFO 存在且 IMDb 一致 | True | 护盾激活，NFO/poster 全部保留 |
| 本地 NFO 存在但 IMDb 不同 | False | 覆盖重写（正确！需要换片） |
| 本地 NFO 不存在 | False | 正常生成 |
| NFO 解析失败（XML 损坏）| False | 保守覆盖（容错） |
| `new_imdb_id` 为空 | False | 无法校验，保守覆盖 |
| 非核级模式（nuclear_reset=False）| 不影响 | Phase 1 护盾参数默认 False，原逻辑不变 |

---

## 六、变更范围

| 文件 | 修改内容 | 行数变化 |
|------|---------|----------|
| `scrape_task.py` | `_nuclear_clean_directory` 新增 `protect_metadata` 参数 + 护盾分支 | +6 行 |
| `scrape_task.py` | Step 3.4 