# V16 全链路接线审计报告

**文档编号**：DEV-RECON-014  
**日期**：2026-03-15  
**状态**：✅ 四项修复指令均已在既有代码中实现，无需修复

---

## Phase 1：scan_task.py 扫描链路

`scan_task.py` **从未调用** `ai_identify_media`。`task_data["clean_name"]` 来源：

```
MediaCleaner.clean_and_extract() → file_info["clean_name"] → task_data["clean_name"]
```

NFO 优先级已正确实现：NFO Shift-Left 层在 `db.insert_task()` 之前执行
`task_data["clean_name"] = nfo.title`，无 AI 反向覆盖路径。✅

---

## Phase 2：scrape_task.py 刮削链路

```python
# line ~144
clean_name = task.get("clean_name", "")  # 从数据库读取 ✅

# line ~261
refined_query = (ai_result.get("query") or "").strip()
if not refined_query:
    refined_query = (clean_name or file_name or "").strip()  # clean_name 兜底 ✅
```

`query` 仅用于 TMDB 搜索，不写入数据库。✅

---

## Phase 3：海报白嫖逻辑

```python
# scan_task.py 通用层
if task_data.get("status") == "archived":  # NFO 注入后触发 ✅
    for _poster_name in ["poster.jpg", "poster.png"]:
        if os.path.exists(os.path.join(_poster_dir, _poster_name)):
            task_data["local_poster_path"] = _poster_candidate
            break
```

NFO 注入 → status="archived" → 海报绑定触发，链路完整。✅

---

## Phase 4：manual_rebuild 400 熔断

```python
# line 1090-1098 — tmdb_id fallback 已实现
new_tmdb_id = body.tmdb_id
if not new_tmdb_id:
    _db_tmdb = task_record.get("tmdb_id")
    if _db_tmdb:
        new_tmdb_id = int(_db_tmdb)  # 从数据库读取 ✅

# 400 触发点均为合理防御边界
# line 1066: target_path 为空 → 无法操作任何文件
# line 1141: nuclear_reset 无视频文件 → 中止核重置
```

✅

---

## 质检：task_data["query"] 非法插入

全库搜索 `task_data["query"]` → **0 处命中** ✅

---

## 流程示意图

```
扫描（scan_task.py）
  MediaCleaner → file_info[clean_name]
    → task_data[clean_name]
    → [NFO] 覆写 task_data[clean_name]=nfo.title  ← 最高优先
    → [海报] status=="archived" → 绑定 poster.jpg
    → db.insert_task()

刮削（scrape_task.py）
  task[clean_name] ← DB 读取
    → [NFO短路] 有NFO → archived, continue
    → MediaCleaner → cleaned_filename
    → ai_identify_media() → {query, year, type}
    → refined_query = query or clean_name  ← 搜索词，不写DB
    → TMDB搜索 → db.update_task_status(title, tmdb_id)

manual_rebuild
  body.tmdb_id → 空 → task_record[tmdb_id] fallback
    → TMDB详情 → 补录海报/字幕/NFO
```

*Neon Crate | DEV-RECON-014 | 2026-03-15*
